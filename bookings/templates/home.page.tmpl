{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>This is the home page</h1>
                <p>This is the test line</p>

                <p>
                    {{index .StringMap "test"}}
                </p>

                <img src="/static/images/outside.png" alt="">
            </div>
        </div>
    </div>
{{end}}
